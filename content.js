// Placeholder content script for future enhancements.
// Currently logic is injected directly from popup using chrome.scripting.executeScript.
// You can use this file later for DOM helpers or message listeners if needed.

// Example message listener (inactive in current flow):
// chrome.runtime.onMessage.addListener((message, _sender, sendResponse) => {
//     if (message?.type === 'PING') {
//         sendResponse({ ok: true });
//     }
// });


